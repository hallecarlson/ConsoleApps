/*#pragma once
#include _USE_MATH_DEFINES
#include <math.h>
using namespace std;

class square
{
public:
	square();
	square(float s);
	square(square& S);
	void setDimensions(float side);
	float getS();
	float perimiter();
	float area();
private:
	float s;
};*/